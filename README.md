# 🎮 Roblox Telegram Bot с веб-приложением

Полнофункциональный Telegram-бот с веб-приложением для заработка Robux через выполнение заданий, реферальную систему и мини-игры.

## 🚀 Возможности

### Telegram Bot
- Реферальная система
- Автоматическая регистрация пользователей
- Интеграция с веб-приложением через Telegram Web App
- Система ключей дня

### Веб-приложение
- 🎯 Кликер с системой ключей
- 📋 Ежедневные задания
- 🗓️ Адвент-календарь (30 дней)
- 🎡 Колесо фортуны
- 🎁 Система кейсов
- 💎 Промокоды
- 👤 Профиль пользователя
- 📊 Статистика

### Админ-панель
- Управление пользователями
- Настройка заданий и наград
- Создание промокодов
- Статистика проекта

## 📦 Установка

### 1. Клонирование и установка зависимостей

```bash
npm install
```

### 2. Настройка базы данных

Создайте PostgreSQL базу данных:

```sql
CREATE DATABASE roblox_bot;
```

### 3. Настройка переменных окружения

Скопируйте `.env.example` в `.env` и заполните необходимые данные:

```bash
cp .env.example .env
```

### 4. Инициализация базы данных

```bash
npm run init:db
```

### 5. Запуск проекта

Для разработки:
```bash
npm run dev:bot    # Запуск бота
npm run dev:api    # Запуск API сервера
```

Для продакшена:
```bash
npm start          # Запуск бота
npm run api        # Запуск API сервера
```

## 🌐 Деплой

### Telegram Bot
- Можно развернуть на любом хостинге с поддержкой Node.js
- Рекомендуется: Heroku, Railway, DigitalOcean

### API Server
- Развертывается отдельно на любом хостинге с Node.js
- Должен быть доступен для бота и веб-приложения

### Веб-приложение (Sweb.ru)
- Загрузите файлы из папки `public/` на Sweb.ru
- Настройте переменные в `public/js/config.js`

## 🔧 Конфигурация

### Настройка бота

1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Получите токен и укажите в `.env`
3. Настройте Web App URL в настройках бота

### Настройка призов

Призы для колеса фортуны и кейсов настраиваются в:
- `src/config/prizes.js` - конфигурация призов
- Через админ-панель после запуска

## 📊 Структура проекта

```
roblox/
├── src/
│   ├── bot/              # Telegram bot
│   ├── api/              # REST API
│   ├── database/         # База данных и модели
│   ├── config/           # Конфигурация
│   └── utils/            # Утилиты
├── public/               # Веб-приложение
│   ├── index.html
│   ├── admin.html
│   ├── css/
│   ├── js/
│   └── assets/
└── docs/                 # Документация

```

## 🔐 Безопасность

- Все пароли хэшируются с помощью bcrypt
- JWT токены для авторизации API
- Rate limiting для защиты от DDoS
- Валидация всех входных данных
- Защита от SQL-инъекций через Sequelize ORM

## 📱 API Endpoints

### Пользователи
- `POST /api/auth` - Авторизация через Telegram
- `GET /api/user/:id` - Получить данные пользователя
- `PUT /api/user/:id/balance` - Обновить баланс

### Игровые механики
- `POST /api/click` - Клик в кликере
- `GET /api/quests` - Получить задания
- `POST /api/quest/complete` - Завершить задание
- `POST /api/wheel/spin` - Крутить колесо
- `POST /api/case/open` - Открыть кейс
- `GET /api/calendar` - Получить адвент-календарь
- `POST /api/calendar/claim` - Забрать награду дня

### Админ
- `POST /api/admin/login` - Авторизация админа
- `GET /api/admin/users` - Список пользователей
- `PUT /api/admin/user/:id` - Изменить пользователя
- `POST /api/admin/promocode` - Создать промокод

## 🎨 Дизайн

Используется тематика Roblox:
- Основной цвет: Фиолетово-синий градиент
- Акцент: Золотой для премиум элементов
- Адаптивный дизайн для всех устройств
- Плавные анимации и переходы

## 📄 Лицензия

ISC

## 👨‍💻 Поддержка

При возникновении проблем создайте issue или свяжитесь с разработчиком.
