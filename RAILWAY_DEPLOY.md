# 🚀 Развертывание на Railway.app

## Шаг 1: Подготовка GitHub

1. Создайте репозиторий на GitHub
2. Загрузите весь код проекта:
```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/roblox-bot.git
git push -u origin main
```

---

## Шаг 2: Создание проекта на Railway

1. Перейдите на [railway.app](https://railway.app)
2. Нажмите **"Start New Project"**
3. Выберите **"Deploy from GitHub"**
4. Авторизуйтесь через GitHub
5. Выберите ваш репозиторий `roblox-bot`
6. Railway автоматически обнаружит Node.js проект

---

## Шаг 3: Настройка переменных окружения

После создания проекта на Railway:

1. Откройте **"Variables"** в панели проекта
2. Добавьте переменные окружения:

```
BOT_TOKEN=YOUR_TELEGRAM_BOT_TOKEN
DB_HOST=your-postgres-host
DB_PORT=5432
DB_NAME=roblox_db
DB_USER=postgres
DB_PASSWORD=your_password
JWT_SECRET=your_secret_key_here
ADMIN_IDS=123456789,987654321
REFS_FOR_KEY=2
KEY_DURATION_HOURS=24
WEB_APP_URL=https://knyaz1.ru/public/index.html
ENVIRONMENT=production
```

### Получение значений:

**BOT_TOKEN:**
- Напишите @BotFather в Telegram
- Выполните команду `/newbot`
- Скопируйте токен

**DB переменные:**
- Если используете Railway PostgreSQL (рекомендуется):
  - Создайте PostgreSQL плагин в Railway
  - Railway автоматически создаст переменные: `DATABASE_URL`
  - Распарсите URL или Railway создаст отдельные переменные

**JWT_SECRET:**
- Любая длинная случайная строка (32+ символа)
- Пример: `super_secret_key_2024_random_string`

**ADMIN_IDS:**
- Ваш Telegram ID (можно найти через @userinfobot)
- Если несколько: `123456789,987654321,555666777`

**WEB_APP_URL:**
- Ваш адрес веб-приложения: `https://knyaz1.ru/public/index.html`

---

## Шаг 4: Подключение PostgreSQL

Railway предоставляет бесплатную PostgreSQL!

1. В Railway проекте нажмите **"+ Create"**
2. Выберите **"Database"** → **"PostgreSQL"**
3. Railway автоматически создаст переменные окружения

Или используйте внешнюю БД:
- ElephantSQL (бесплатно)
- Supabase (бесплатно)
- AWS RDS (платно)

---

## Шаг 5: Инициализация БД

После настройки переменных нужно инициализировать БД:

**Способ 1: Через Railway CLI**
```bash
npm install -g @railway/cli
railway login
railway run npm run init:db
```

**Способ 2: Через веб-интерфейс**
1. В Railway откройте **"Deploy Logs"**
2. Дождитесь завершения развертывания
3. Откройте **"Deployments"** → недавний деплой
4. В консоли выполните инициализацию БД

---

## Шаг 6: Получение URL API

После успешного развертывания:

1. Откройте Railway проект
2. Нажмите на сервис (обычно называется как папка или "api")
3. Скопируйте URL вида:
   ```
   https://your-project.up.railway.app
   ```

---

## Шаг 7: Обновление конфига веб-приложения

Обновите `public/js/config.js` на вашем sweb.ru:

```javascript
const CONFIG = {
  API_URL: 'https://your-project.up.railway.app/api',
  APP_NAME: 'Roblox Robux',
  VERSION: '1.0.0'
};
```

Замените `your-project` на имя вашего Railway проекта!

---

## Шаг 8: Запуск бота (опционально)

Если хотите запускать бота на том же сервере:

1. Добавьте в Railway еще один сервис
2. Измените команду запуска на `npm run bot`
3. Используйте те же переменные окружения

Или запустите бота локально на своем компьютере.

---

## 🔍 Проверка статуса

После развертывания:

1. **API должна быть доступна:**
   ```
   https://your-project.up.railway.app/api/health
   ```
   Должен вернуть статус 200

2. **Веб-приложение должно работать:**
   - Откройте `https://knyaz1.ru/public/index.html`
   - Должны видеть приложение
   - При клике на кнопку должно работать

3. **Проверьте логи в Railway:**
   - Откройте **"Logs"** в проекте
   - Ищите ошибки или сообщения подключения

---

## 🚨 Решение проблем

### "Cannot find module"
- Убедитесь, что все файлы загружены на GitHub
- Проверьте структуру папок

### "Database connection failed"
- Проверьте переменные окружения в Railway
- Убедитесь, что PostgreSQL создана и запущена
- Инициализируйте БД командой `npm run init:db`

### "CORS error"
- Проверьте `DB_HOST` и другие DB параметры
- Убедитесь, что веб-приложение обращается к правильному API URL

### "Token invalid"
- Проверьте BOT_TOKEN в переменных окружения
- Убедитесь, что токен скопирован полностью без пробелов

---

## 💡 Советы

1. **Используйте Railway PostgreSQL** - она бесплатна и интегрирована
2. **Сохраняйте переменные в .env локально** для развития
3. **Проверяйте логи** при проблемах
4. **Используйте Railway CLI** для быстрого управления

---

## 📊 Стоимость

**Railway (бесплатно!):**
- 5$ credits/месяц = примерно 100-200 часов использования
- PostgreSQL: 7$ credits/месяц
- **ИТОГО:** Может быть абсолютно бесплатно при умном использовании!

---

## ✅ Готово!

После этих шагов ваш API будет доступен по адресу:
```
https://your-project.up.railway.app/api
```

А веб-приложение работает на:
```
https://knyaz1.ru/public/index.html
```

Поздравляем! Ваш Roblox бот в продакшене! 🎉
