# 📘 Руководство по развертыванию проекта

## 🎯 Структура проекта

```
roblox/
├── src/
│   ├── api/
│   │   ├── server.js           # Express API сервер
│   │   └── routes/             # API маршруты
│   │       ├── auth.js         # Авторизация
│   │       ├── user.js         # Пользователи
│   │       ├── game.js         # Игровые механики
│   │       └── admin.js        # Админ-панель
│   ├── bot/
│   │   └── index.js            # Telegram бот
│   ├── database/
│   │   ├── connection.js       # Подключение к БД
│   │   ├── models/             # Модели Sequelize
│   │   └── init.js             # Инициализация БД
│   ├── config/
│   │   └── prizes.js           # Конфигурация призов
│   └── utils/
│       └── helpers.js          # Вспомогательные функции
├── public/                     # Веб-приложение (для Sweb.ru)
│   ├── index.html              # Главная страница
│   ├── admin.html              # Админ-панель
│   ├── css/
│   │   ├── style.css           # Стили приложения
│   │   └── admin.css           # Стили админки
│   ├── js/
│   │   ├── config.js           # Конфигурация
│   │   ├── api.js              # API клиент
│   │   ├── app.js              # Основное приложение
│   │   ├── clicker.js          # Кликер
│   │   ├── quests.js           # Задания
│   │   ├── wheel.js            # Колесо фортуны
│   │   ├── calendar.js         # Календарь
│   │   ├── cases.js            # Кейсы
│   │   └── admin.js            # Админка
│   └── assets/                 # Изображения
├── package.json
├── .env                        # Переменные окружения
└── README.md
```

## 🚀 Шаг 1: Установка зависимостей

### 1.1 Установите Node.js
Скачайте и установите Node.js с официального сайта: https://nodejs.org/ (версия 16 или выше)

### 1.2 Установите PostgreSQL
Скачайте и установите PostgreSQL: https://www.postgresql.org/download/

### 1.3 Установите зависимости проекта

```powershell
cd C:\Users\user\Desktop\roblox
npm install
```

## 🗄️ Шаг 2: Настройка базы данных

### 2.1 Создайте базу данных PostgreSQL

Откройте pgAdmin или выполните в командной строке PostgreSQL:

```sql
CREATE DATABASE roblox_bot;
CREATE USER roblox_user WITH PASSWORD 'your_secure_password';
GRANT ALL PRIVILEGES ON DATABASE roblox_bot TO roblox_user;
```

### 2.2 Настройте файл .env

Скопируйте `.env.example` в `.env`:

```powershell
Copy-Item .env.example .env
```

Отредактируйте `.env` и укажите ваши данные:

```env
# Telegram Bot
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
BOT_USERNAME=YourBotUsername

# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=roblox_bot
DB_USER=roblox_user
DB_PASSWORD=your_secure_password

# API
API_PORT=3000
API_HOST=localhost
API_URL=http://localhost:3000

# Web App (замените на ваш домен на Sweb.ru)
WEB_APP_URL=https://your-domain.sweb.ru

# Security
JWT_SECRET=your_random_jwt_secret_key_here_32_chars_minimum
ADMIN_PASSWORD=your_admin_password

# Admin IDs (укажите свой Telegram ID)
ADMIN_IDS=123456789

# Settings
REFS_FOR_KEY=2
KEY_DURATION_HOURS=24
CLICK_REWARD=1
```

### 2.3 Инициализируйте базу данных

```powershell
npm run init:db
```

Эта команда создаст все таблицы и заполнит базовые данные (задания, призы, календарь).

## 🤖 Шаг 3: Создание и настройка Telegram бота

### 3.1 Создайте бота через BotFather

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Укажите имя бота (например: "Roblox Robux Bot")
4. Укажите username бота (например: "roblox_robux_bot")
5. Скопируйте токен и вставьте в `.env` как `BOT_TOKEN`

### 3.2 Настройте Web App

1. Отправьте `/mybots` в BotFather
2. Выберите вашего бота
3. Выберите "Bot Settings" → "Menu Button"
4. Выберите "Configure menu button"
5. Укажите текст: "🎮 Открыть приложение"
6. Укажите URL: `https://your-domain.sweb.ru` (замените на ваш домен)

### 3.3 Получите свой Telegram ID

1. Откройте [@userinfobot](https://t.me/userinfobot) в Telegram
2. Бот пришлет ваш ID
3. Добавьте его в `.env` в параметр `ADMIN_IDS`

## 💻 Шаг 4: Запуск локально (для тестирования)

### 4.1 Запустите API сервер

```powershell
npm run dev:api
```

API будет доступен на http://localhost:3000

### 4.2 Запустите бота (в другом окне PowerShell)

```powershell
npm run dev:bot
```

Бот запустится и будет готов к работе.

### 4.3 Откройте веб-приложение локально

Для локального тестирования можно использовать любой веб-сервер, например:

```powershell
cd public
python -m http.server 8000
```

Откройте http://localhost:8000 в браузере.

**Важно:** Для полноценного тестирования Telegram Web App нужен HTTPS и домен.

## 🌐 Шаг 5: Развертывание на продакшн

### 5.1 API и Бот (любой хостинг с Node.js)

Рекомендуемые хостинги:
- **Heroku** (бесплатный тариф)
- **Railway.app** (бесплатный тариф)
- **DigitalOcean** (от $5/месяц)
- **VPS** от любого провайдера

#### Пример развертывания на Railway:

1. Зарегистрируйтесь на https://railway.app
2. Создайте новый проект "New Project" → "Deploy from GitHub"
3. Подключите ваш GitHub репозиторий
4. Добавьте PostgreSQL: "New" → "Database" → "PostgreSQL"
5. Настройте переменные окружения в разделе "Variables"
6. Railway автоматически развернет ваше приложение

#### Для запуска на VPS:

```bash
# Установите PM2 для управления процессами
npm install -g pm2

# Запустите API
pm2 start src/api/server.js --name roblox-api

# Запустите бота
pm2 start src/bot/index.js --name roblox-bot

# Сохраните конфигурацию
pm2 save
pm2 startup
```

### 5.2 Веб-приложение на Sweb.ru

1. **Зарегистрируйтесь на Sweb.ru**
   - Откройте https://sweb.ru
   - Создайте аккаунт и выберите план хостинга

2. **Загрузите файлы**
   - Подключитесь по FTP или через файловый менеджер
   - Загрузите ВСЕ файлы из папки `public/` в корень домена

3. **Настройте config.js**
   
   Откройте `public/js/config.js` и укажите URL вашего API:

   ```javascript
   const CONFIG = {
     API_URL: 'https://your-api-domain.com/api',
     APP_NAME: 'Roblox Robux',
     VERSION: '1.0.0'
   };
   ```

4. **Настройте HTTPS**
   - В панели управления Sweb.ru включите SSL сертификат
   - Это необходимо для работы Telegram Web App

### 5.3 Обновите настройки бота

После развертывания обновите переменные окружения:

```env
API_URL=https://your-api-domain.com
WEB_APP_URL=https://your-domain.sweb.ru
```

И перезапустите бота:

```bash
pm2 restart roblox-bot
```

## 🧪 Шаг 6: Тестирование

### 6.1 Протестируйте бота

1. Откройте бота в Telegram
2. Отправьте `/start`
3. Проверьте, что отображается приветственное сообщение
4. Нажмите "Открыть приложение"

### 6.2 Протестируйте функции

- ✅ Авторизация через Telegram
- ✅ Отображение баланса
- ✅ Кликер (с активным ключом)
- ✅ Задания
- ✅ Колесо фортуны
- ✅ Адвент-календарь
- ✅ Кейсы
- ✅ Промокоды
- ✅ Реферальная система

### 6.3 Протестируйте админ-панель

1. Откройте `https://your-domain.sweb.ru/admin.html`
2. Проверьте статистику
3. Попробуйте изменить баланс пользователя
4. Создайте промокод

## 🔧 Шаг 7: Дополнительные настройки

### 7.1 Настройка призов

Призы настраиваются через админ-панель или напрямую в базе данных.

Для изменения шансов откройте `src/config/prizes.js` и отредактируйте значения.

### 7.2 Настройка заданий

Создавайте новые задания через админ-панель в разделе "Задания".

### 7.3 Настройка адвент-календаря

Редактируйте награды календаря через админ-панель в разделе "Календарь".

### 7.4 Бэкап базы данных

Настройте регулярные бэкапы PostgreSQL:

```bash
# Создание бэкапа
pg_dump -U roblox_user roblox_bot > backup_$(date +%Y%m%d).sql

# Восстановление из бэкапа
psql -U roblox_user roblox_bot < backup_20241204.sql
```

## 📊 Мониторинг и логи

### Просмотр логов PM2:

```bash
pm2 logs roblox-api
pm2 logs roblox-bot
```

### Мониторинг процессов:

```bash
pm2 monit
```

### Перезапуск при изменениях:

```bash
pm2 restart roblox-api
pm2 restart roblox-bot
```

## 🔒 Безопасность

1. **Никогда не публикуйте `.env` файл**
2. **Используйте сильные пароли** для базы данных
3. **Регулярно обновляйте зависимости**: `npm update`
4. **Настройте firewall** на сервере
5. **Используйте HTTPS** для всех соединений
6. **Ограничьте доступ к админ-панели** (можно добавить базовую HTTP авторизацию)

## 🐛 Решение проблем

### Бот не отвечает
- Проверьте, что бот запущен: `pm2 status`
- Проверьте логи: `pm2 logs roblox-bot`
- Убедитесь, что токен правильный в `.env`

### Ошибки базы данных
- Проверьте подключение к PostgreSQL
- Убедитесь, что таблицы созданы: `npm run init:db`
- Проверьте логи API: `pm2 logs roblox-api`

### Веб-приложение не открывается
- Проверьте, что файлы загружены на Sweb.ru
- Убедитесь, что `config.js` содержит правильный API URL
- Проверьте консоль браузера (F12) на наличие ошибок

### Проблемы с авторизацией
- Проверьте, что `BOT_TOKEN` правильный
- Убедитесь, что `JWT_SECRET` установлен
- Проверьте, что API доступен по HTTPS

## 📞 Поддержка

Если у вас возникли вопросы или проблемы:

1. Проверьте логи сервера и бота
2. Убедитесь, что все переменные окружения настроены
3. Перезапустите сервисы: `pm2 restart all`

## 🎉 Готово!

Ваш Telegram бот с веб-приложением готов к использованию!

Теперь вы можете:
- Приглашать пользователей по реферальной ссылке
- Создавать промокоды для раздачи
- Настраивать задания и награды
- Отслеживать статистику в админ-панели

Удачи в развитии вашего проекта! 🚀
